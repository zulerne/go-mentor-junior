# All variables are optional — defaults are used if not set.
# Copy to .env for local overrides: cp .env.example .env

# Service environment: local | production
# ENV=local

# HTTP server
# HTTP_ADDRESS=:8080
# HTTP_TIMEOUT=5s
# HTTP_IDLETIMEOUT=60s
# HTTP_SHUTDOWNTIMEOUT=10s
